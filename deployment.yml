---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy
  annotations:
    kubernetes.io/change-cause: "cost estimator image added"
spec:
  replicas: 5
  selector:
    matchLabels:
      app: swiggy
  template:
    metadata:
      labels:
        app: swiggy
    spec:
      containers:
        - name: cont3
          image: venkat2708/train-ticket-booking:v2
          ports:
            - containerPort: 80
